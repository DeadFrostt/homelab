apiVersion: apps/v1
kind: Deployment
metadata:
  name: rac-outpost
  namespace: authentik
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rac-outpost
  template:
    metadata:
      labels:
        app: rac-outpost
    spec:
      containers:
        - name: rac
          image: ghcr.io/goauthentik/rac:2026.5.5@sha256:7ccd6c5e45eb3ae45e7d7392bd25899c4d1ff601f6d630bfab7528236328aba7
          ports:
            - containerPort: 4822
              name: guacd
          env:
            - name: AUTHENTIK_HOST
              value: "http://authentik-server:9000"
            - name: AUTHENTIK_INSECURE
              value: "false"
            - name: AUTHENTIK_TOKEN
              valueFrom:
                secretKeyRef:
                  name: authentik-env
                  key: AUTHENTIK_TOKEN
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "64Mi"
              cpu: "25m"
---
apiVersion: v1
kind: Service
metadata:
  name: rac-outpost
  namespace: authentik
spec:
  selector:
    app: rac-outpost
  ports:
    - port: 4822
      targetPort: 4822
      name: guacd
  type: ClusterIP
