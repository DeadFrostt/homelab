apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trawl-redis
  namespace: trawl
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
