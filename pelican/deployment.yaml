apiVersion: apps/v1
kind: Deployment
metadata:
  name: panel
  namespace: pelican
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: panel
  template:
    metadata:
      labels:
        app: panel
    spec:
      containers:
        - name: panel
          image: ghcr.io/pelican-dev/panel:latest@sha256:e08ccc14837f9884f7b51756398b1682c26b4c6cf1d0b795b17f2405b1b58df2
          ports:
            - containerPort: 80
              name: http
          env:
            - name: XDG_DATA_HOME
              value: /pelican-data
            - name: APP_URL
              value: "https://panel.deadfrost.dev"
            - name: ASSET_URL
              value: "https://panel.deadfrost.dev"
            - name: BEHIND_PROXY
              value: "true"
            - name: TRUSTED_PROXIES
              value: "0.0.0.0/0,::/0"
            - name: ADMIN_EMAIL
              value: "__ADMIN_EMAIL__"
            - name: APP_ENV
              value: "production"
            - name: APP_DEBUG
              value: "false"
          envFrom:
            - secretRef:
                name: pelican-env
          volumeMounts:
            - name: data
              mountPath: /pelican-data
            - name: logs
              mountPath: /var/www/html/storage/logs
          livenessProbe:
            httpGet:
              path: /up
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /up
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            limits:
              memory: "640Mi"
              cpu: "750m"
            requests:
              memory: "128Mi"
              cpu: "50m"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pelican-data
        - name: logs
          persistentVolumeClaim:
            claimName: pelican-logs
---
apiVersion: v1
kind: Service
metadata:
  name: panel
  namespace: pelican
spec:
  type: NodePort
  selector:
    app: panel
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30180
