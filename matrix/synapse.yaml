apiVersion: apps/v1
kind: Deployment
metadata:
  name: synapse
  namespace: matrix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: synapse
  template:
    metadata:
      labels:
        app: synapse
    spec:
      containers:
        - name: synapse
          image: matrixdotorg/synapse:latest@sha256:6965329e03e72f170c9f7e201e83d5f6e06116aae0431a76634dd84d4f70b106
          ports:
            - containerPort: 8008
              name: client
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            limits:
              memory: "2Gi"
              cpu: "750m"
            requests:
              memory: "512Mi"
              cpu: "100m"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: matrix-synapse
---
apiVersion: v1
kind: Service
metadata:
  name: synapse
  namespace: matrix
spec:
  type: NodePort
  selector:
    app: synapse
  ports:
    - name: client
      port: 8008
      targetPort: 8008
      nodePort: 30899
